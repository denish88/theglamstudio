const User = require('./user.model')
const Post = require('./post.model')
const Directory = require('./directory.model')
const Like = require('./like.model')

module.exports = { User, Post, Directory, Like }
